  // * twiddleCtrl (needs reset,enable) + setup
  // overall setup done on right clk cycle + using counter (matches IOen)
  // * calcControl (needs reset,enable)
  // * memBanks (mem needs enable)
  // fix frameout timing
  // 3 multiplies
  // mem readen, test 100?
  // w0z