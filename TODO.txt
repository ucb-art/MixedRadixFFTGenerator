  // * twiddleCtrl (needs reset,enable) + setup
  // overall setup done on right clk cycle + using counter (matches IOen) -- hold setupdone
  // * calcControl (needs reset,enable)
  // * memBanks (mem needs enable)
  // fix frameout timing
  // 3 multiplies
  // mem readen, test 100?
  // w0z
  // tilelink







currentStage

use default counter:


wrapcond when out = maxused
inc = 1

both need custom change cond -- also ioEnable
!calcDoneFlag !discardCalcWrite
above & subcounterwrap

globalreset = calcResetCond??? (startFirstFrame, ioWrap -- is that inclusive?)

---


use stagePrimeIdx


